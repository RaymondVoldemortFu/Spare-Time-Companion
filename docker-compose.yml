services:
  companion:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////app/data/app.db
      MODEL_NAME: qwen3.7-plus
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    ports:
      - "80:8000"
    restart: unless-stopped
