# Spare-Time Companion 云端服务端需求文档

## 项目定位

Spare-Time Companion 的服务端需要承担产品的主要智能能力。真实桌面客户端只负责采集用户输入、播放云端返回的语音、显示拟人化表情和上传基础交互事件。服务端负责语音理解、自然语言对话、上下文分析、LLM Agent 决策、语音生成和长期交互记录。

服务端不是一个单纯的推荐接口，也不是一个固定规则的提醒系统，而是一个面向桌面陪伴场景的云端语音对话系统。用户可以像和一个桌面伙伴说话一样与设备交互，系统需要理解用户当前状态、结合历史上下文作出自然回应，并在合适时给出轻量建议。

## 客户端与服务端边界

真实客户端不展示自然语言文本。客户端屏幕只用于拟人化表情、状态动画和简单视觉反馈。自然语言内容必须通过云端生成音频后由客户端播放。

客户端不承担语音识别、语音合成、自然语言理解、长期记忆、用户画像分析或活动建议决策。上述能力都由服务端完成。

服务端可以生成和保存文本内容，但这些文本只用于语音合成、后台调试、虚拟客户端测试和历史记录，不作为真实客户端屏幕展示内容。

## 语音交互需求

服务端需要支持完整的语音交互闭环。用户通过客户端说话，客户端上传音频，服务端完成语音转文字，再将识别结果交给 LLM Agent 处理。LLM Agent 生成回应后，服务端需要完成文字转语音，并把可播放音频返回给客户端。

语音是正式产品中的主要交互方式。服务端需要保证用户可以通过连续语音对话表达问题、状态、偏好、否定、补充和反馈。系统不能只支持一次性命令，也不能把用户所有输入都处理成活动推荐。

当语音识别失败、语音合成失败或云端处理失败时，服务端需要返回客户端可理解的状态，使客户端能够通过表情、动画或提示音表达失败，而不是依赖文本错误提示。

## 自然语言对话需求

服务端需要支持连续自然语言对话。用户可以进行普通聊天，可以询问问题，可以表达“我不知道现在该做什么”，也可以否定上一条建议、要求重新生成、补充个人偏好或让系统记住某件事。

LLM Agent 需要根据当前对话语境决定本轮应该如何回应。系统不能把所有用户输入都强行转成推荐任务，也不能用固定流程判断用户是否“该被推荐”。在很多情况下，合理回应可能只是普通聊天、确认用户状态、追问一句、记录用户偏好，或者保持安静。

对话系统需要保留上下文连续性。用户在上一轮表达过的状态、刚刚拒绝过的建议、近期提到的兴趣或愿望，都应能影响后续回应。

## LLM Agent 决策需求

服务端的核心决策应由 LLM Agent 完成。LLM Agent 需要在每轮交互中基于用户输入、对话历史、桌面状态、用户偏好、愿望清单、近期反馈和外部上下文进行自主推理。

LLM Agent 不应被固定成“状态分类、候选生成、候选筛选、输出推荐”的死流程。系统需要允许 Agent 在 ReAct 式循环中自行判断是否需要调用上下文工具、是否需要查历史记录、是否需要读取愿望清单、是否需要参考天气或日程、是否需要更新记忆，以及最终是回复、追问、推荐、确认、记录还是不主动输出。

服务端需要保存 LLM Agent 的运行摘要，用于调试和复盘，但不要求暴露完整链式思考。记录重点应包括本轮输入来源、使用过的上下文、调用过的工具、最终动作、生成的语音内容、音频生成状态和用户后续反馈。

## 用户上下文需求

服务端需要维护用户上下文，使 LLM Agent 能够进行个性化对话。上下文应包含用户明确表达过的偏好、愿望清单、近期对话、近期反馈、桌面使用状态和与当前场景相关的外部信息。

用户画像和行为分析主要由 LLM 基于有限上下文完成。课程项目中的用户数据量不足以支撑传统大规模推荐系统、协同过滤或复杂统计建模，因此不要求构建搜索广告推荐式系统。服务端应把有限历史整理成可供 LLM 使用的上下文，让 LLM 完成理解和判断。

用户上下文不应只服务于推荐，也应服务于普通对话、记忆更新、活动延续、反馈理解和状态确认。

## 桌面状态需求

服务端需要接收桌面端上传的基础状态，并将其作为 LLM Agent 的上下文。桌面状态的作用是帮助 Agent 理解当前交互发生在什么场景下，而不是让服务端根据固定字段写死判断规则。

例如，用户是否在桌前、是否长时间没有交互、是否刚结束学习、是否主动触发响应、设备当前是否正在播放音频等状态，都可以影响 LLM Agent 的理解。但这些状态不应直接对应固定输出，而应由 LLM Agent 结合对话和历史上下文综合判断。

## 主动触发响应需求

客户端需要支持用户主动触发响应。主动触发响应可以来自按钮、语音唤醒或虚拟客户端的测试操作。

主动触发响应不等于“抽卡”，也不等于固定生成活动建议。它表示用户希望系统在当前上下文下作出回应。服务端需要把该事件交给 LLM Agent，由 Agent 判断本轮应该进行自然回复、给出建议、追问用户、继续上一轮对话、重新生成回应或执行其他合理动作。

如果主动触发响应同时伴随用户语音输入，服务端需要将语音内容和主动触发事件作为同一轮交互理解。

## 活动建议需求

服务端需要支持活动建议，但活动建议只是自然语言对话的一种可能结果，而不是系统唯一目标。

当用户表达无聊、迷茫、想休息、不知道做什么，或者主动请求建议时，LLM Agent 可以结合用户上下文生成合适的活动建议。建议内容应由 LLM 自主判断，不在需求文档中预设活动类别，也不要求服务端按固定标签筛选。

建议需要能够被语音自然表达，并适合桌面陪伴设备播报。真实客户端不展示建议正文或推荐理由，相关文本只用于语音合成、后台记录和虚拟客户端测试。

## 记忆与反馈需求

服务端需要支持用户记忆。用户明确表达的偏好、厌恶、愿望、长期目标、近期计划和对系统回应的评价，都应能够被记录，并在后续对话中被 LLM Agent 使用。

服务端需要接收用户反馈。反馈可以来自语音，也可以来自按钮或虚拟客户端操作。反馈不应只作为统计结果保存，而应成为后续 Agent 推理的上下文。用户刚刚否定过的回应、要求重新生成的内容、明确表示不喜欢的建议，都应影响后续交互。

记忆更新应由 LLM Agent 根据对话语义判断。用户明确要求记住的信息应被记录；由系统推断出的偏好也可以记录，但需要保留来源，方便调试。

## 外部上下文需求

服务端需要支持接入必要外部上下文，例如当前时间、天气、日程和节假日信息。这些信息用于帮助 LLM Agent 理解当前场景，而不是让系统依赖固定规则生成建议。

课程项目中可以使用真实 API，也可以使用模拟数据。无论采用哪种方式，外部上下文都应以稳定形式提供给 LLM Agent。外部信息不可用时，服务端仍应允许 Agent 基于已有上下文完成合理回应。

## 音频与表情输出需求

服务端每轮有用户可感知输出时，需要生成语音播报内容并完成云端语音合成。客户端接收音频后播放。

服务端还需要返回拟人化表情状态和动画状态，使客户端能够在听、想、说、等待、困惑、错误、待机等场景下显示对应视觉反馈。

真实客户端的输出由“音频 + 表情/动画”构成。文本不属于真实客户端的用户输出方式。

## API 需求

服务端需要提供稳定、清晰、可测试的 API，使真实客户端、虚拟客户端和管理后台能够完成交互。

API 需求重点是语义稳定，而不是字段细节。服务端需要保证音频上传、语音对话、主动触发响应、状态上报、反馈提交、历史查询和测试调试等能力能够被客户端可靠调用。

所有 API 应具备统一的请求与响应规范、统一错误格式、版本管理和必要认证。LLM 输出不能破坏 API 稳定性。真实客户端相关接口不得要求客户端展示自然语言文本。虚拟客户端和后台可以获取调试文本、转写结果、语音播报文本和运行摘要。

API 文档需要说明各类接口的用途、调用场景、成功结果、失败结果和客户端应如何处理返回状态，但不需要在需求文档中展开每个字段。

## 虚拟客户端测试需求

服务端需要支持虚拟桌面端进行在线测试。虚拟客户端用于在没有真实硬件的情况下测试服务端能力。

虚拟客户端可以上传音频、发送文本、模拟主动触发、模拟桌面状态、播放返回音频、展示表情状态、提交用户反馈，并查看完整请求和响应。虚拟客户端可以展示文本内容，但这是调试需求，不代表真实客户端具备文本显示能力。

服务端需要通过虚拟客户端验证完整闭环：用户输入进入服务端，经过 STT、LLM Agent 推理、TTS 音频生成，再返回客户端播放，并能够继续提交反馈影响后续交互。


## 安全需求

服务端采用标准 Web 服务安全策略。用户访问、设备访问和后台访问需要具备基本认证与权限控制。音频上传需要限制格式、大小、时长和频率。服务端不得向客户端暴露密钥、系统路径、数据库连接信息或完整错误堆栈。

本课程项目不额外展开复杂隐私治理，但服务端仍需保证不同用户、不同设备的数据不会混淆。

## 性能需求

服务端需要保证真实交互体验可用。设备心跳、状态上报和反馈提交应保持快速响应。语音识别、LLM Agent 推理和语音合成允许存在一定延迟，但完整语音回合不应让用户长时间等待。

完整语音回合从客户端上传音频到客户端获得可播放音频，目标耗时不超过十五秒。文本输入或主动触发响应进入 LLM Agent 后，目标耗时不超过八秒。语音识别和语音合成各自目标耗时不超过五秒。

当 STT、LLM Agent 或 TTS 超时时，服务端必须返回可解析的失败状态，使客户端能够显示错误表情、进入重试状态或播放本地提示音，而不是一直等待。

## 异常处理需求

服务端需要处理常见异常，包括音频不可用、语音识别失败、LLM Agent 失败、语音合成失败、设备未绑定、请求无效、外部上下文不可用和后台权限不足等情况。

异常结果需要保持统一结构，并能被真实客户端转化为表情状态、动画状态、重试状态或本地提示音。错误文本主要用于后台和虚拟客户端，不作为真实客户端展示内容。

## 验收标准

服务端完成后，应能够支持真实或虚拟客户端完成一次完整语音交互：用户输入音频，服务端完成语音识别，LLM Agent 根据上下文进行自然语言推理，服务端生成语音音频，客户端播放音频并显示拟人化表情，用户提交反馈，反馈进入后续上下文。

服务端应能够证明 LLM Agent 不是固定推荐流水线，而是能够根据不同输入自然决定普通回复、追问、建议、记录、确认、重新生成或静默。

服务端应能够证明真实客户端不依赖文本显示，所有用户可感知自然语言输出都通过音频完成，屏幕只承担拟人化表情和状态反馈。

服务端应能够通过虚拟客户端展示和调试完整链路，包括音频上传、STT、LLM Agent、TTS、表情状态、反馈和历史记录。

服务端应提供可用 API 文档，使真实客户端、虚拟客户端和后台能够稳定接入。
